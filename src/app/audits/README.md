# Audits Route

`/audits` provides the authenticated audit trail index.

- `page.tsx`: list of audit events in user-visible scope.
- `[id]/page.tsx`: drilldown details for one audit event, including target and related records.
